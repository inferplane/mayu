/bin/
/inferplane
*.db
*.db-wal
*.db-shm
.claude/*.local.json
coverage.out

# Secrets and local runtime artifacts
.env
.env.*
!.env.example
*.jsonl
*.wal
*.pem
*.key

# co-agent consensus ephemeral state
.claude/co-agent-consensus/

# operator-local notes (never commit)
/secrets

# serena tooling cache
.serena/
